import com.sun.glass.ui.View;

import static java.lang.Math.ceil;

public class Play {

    public static void main(String[] args) {
        /**
         * PARTIE TEST (2 players, 5 boat (1 of each))
         */

        Battle battle = new Battle();

        //battle.partieReseauClient(battle.getGame(),battle.getW());
        //battle.partiePersoReseauHost(battle.getGame(),battle.getW(),1,0,0,0,0);
        //battle.partieClassicReseauHost(battle.getGame(),battle.getW());
        //battle.partieClassicContreOrdi(battle.getGame(),battle.getW());
        //battle.partiePersoContreOrdinateur(battle.getGame(),battle.getW());
    }
}
